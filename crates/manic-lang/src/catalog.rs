//! The **builtin catalog** — machine-readable specs for every manic builtin,
//! plus the fixed vocabularies (colors, easings, presets, reserved vars,
//! keywords). This is what powers editor **highlighting** (classify a name as a
//! builtin), **autocomplete** (offer names + type-appropriate values), and
//! **quick-fixes** (nearest-name suggestions).
//!
//! The engine keeps this honest: a test in the `manic` crate asserts that the
//! catalog's name set equals `Registry::builtins()`, so a new builtin can't ship
//! without a catalog entry (and vice-versa) — no drift.

/// What a builtin is: a constructor/modifier (t=0), a timeline verb, or a
/// stateful (mutating) verb.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Kind {
    Ctor,
    Verb,
    MutVerb,
}

/// The argument type an editor should offer/validate for a parameter.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Ty {
    /// A fresh entity id the author invents (a ctor's first arg).
    Name,
    /// An existing entity id or tag (a verb's target, a geo reference point).
    Ident,
    Num,
    Str,
    /// A `(x, y)` point literal.
    Point,
    /// A `(x, y, z)` point literal.
    Point3,
    /// A palette colour name.
    Color,
    /// An easing name.
    Ease,
    /// A named plot function (`sin`/`cos`/…) — a bareword from [`NAMED_FNS`], or
    /// a `"formula"` string instead (validated only when written as a bareword).
    Fn,
}

#[derive(Debug, Clone)]
pub struct Param {
    pub name: &'static str,
    pub ty: Ty,
    pub optional: bool,
}

#[derive(Debug, Clone)]
pub struct BuiltinSpec {
    pub name: &'static str,
    pub kind: Kind,
    /// Which kit provides it (`std` / `math` / `geo` / `algo` / `brand`).
    pub kit: &'static str,
    pub summary: &'static str,
    pub params: Vec<Param>,
}

fn spec(
    name: &'static str,
    kind: Kind,
    kit: &'static str,
    summary: &'static str,
    params: &[(&'static str, Ty, bool)],
) -> BuiltinSpec {
    BuiltinSpec {
        name,
        kind,
        kit,
        summary,
        params: params
            .iter()
            .map(|&(name, ty, optional)| Param { name, ty, optional })
            .collect(),
    }
}

/// Every builtin, with signatures for the common ones (the long tail carries
/// name/kind/kit/summary — enough for highlighting + name completion — and gains
/// params over time).
pub fn catalog() -> Vec<BuiltinSpec> {
    use Kind::*;
    use Ty::*;
    // (name, ty, optional)
    const O: bool = true;
    const R: bool = false;
    vec![
        // ---- std: shapes ----
        spec(
            "circle",
            Ctor,
            "std",
            "a circle",
            &[("id", Name, R), ("center", Point, R), ("r", Num, R)],
        ),
        spec(
            "rect",
            Ctor,
            "std",
            "a rectangle",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("w", Num, R),
                ("h", Num, R),
            ],
        ),
        spec(
            "image",
            Ctor,
            "std",
            "a raster image (PNG/JPG) from a bundled asset: URI or file path, centred at a point (w×h px)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("path", Str, R),
                ("w", Num, O),
                ("h", Num, O),
            ],
        ),
        spec(
            "equation",
            Ctor,
            "std",
            "typeset a LaTeX math string centred at a point; standard \\textcolor{cyan}{...} gives individual terms semantic Manic colours that follow the template. Put LaTeX in BACKTICKS; animate with show/fade/move/scale (image, not draw-on)",
            &[("id", Name, R), ("center", Point, R), ("latex", Str, R), ("size", Num, O)],
        ),
        spec(
            "line",
            Ctor,
            "std",
            "a line segment",
            &[("id", Name, R), ("from", Point, R), ("to", Point, R)],
        ),
        spec(
            "link",
            Ctor,
            "std",
            "a straight or bent edge that follows two moving entities",
            &[
                ("id", Name, R),
                ("from", Ident, R),
                ("to", Ident, R),
                ("bend", Num, O),
            ],
        ),
        spec(
            "arrow",
            Ctor,
            "std",
            "an arrow",
            &[("id", Name, R), ("from", Point, R), ("to", Point, R)],
        ),
        // variadic points + optional trailing colour — see `c_polygon`
        spec(
            "polygon",
            Ctor,
            "std",
            "a filled polygon: polygon(id, (x1,y1), (x2,y2), (x3,y3), ..., [color])",
            &[],
        ),
        spec(
            "dot",
            Ctor,
            "std",
            "a small filled dot",
            &[("id", Name, R), ("at", Point, R), ("r", Num, O)],
        ),
        spec(
            "particles",
            Ctor,
            "std",
            "deterministic small dots inside a circle or rectangle; optional random/grid/ring layout",
            &[
                ("id", Name, R),
                ("container", Ident, R),
                ("count", Num, R),
                ("radius", Num, O),
                ("seed", Num, O),
                ("layout", Str, O),
            ],
        ),
        spec(
            "text",
            Ctor,
            "std",
            "a text label",
            &[("id", Name, R), ("at", Point, R), ("text", Str, R)],
        ),
        spec("label", Ctor, "std", "a label pinned to an entity", &[]),
        spec(
            "caption",
            Ctor,
            "std",
            "a row of caption words (karaoke/pop)",
            &[
                ("id", Name, R),
                ("words", Str, R),
                ("at", Point, R),
                ("size", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "support",
            Ctor,
            "std",
            "a hatched fixed support (wall/ceiling/floor) for mechanics diagrams; dir = open side",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("len", Num, O),
                ("dir", Str, O),
            ],
        ),
        spec(
            "counter",
            Ctor,
            "std",
            "a live numeric readout",
            &[
                ("id", Name, R),
                ("at", Point, R),
                ("value", Num, R),
                ("decimals", Num, R),
                ("prefix", Str, R),
                ("suffix", Str, R),
            ],
        ),
        spec(
            "parameter",
            Ctor,
            "std",
            "a visible bounded value for a smooth multi-visual journey",
            &[
                ("id", Name, R),
                ("at", Point, R),
                ("initial", Num, R),
                ("min", Num, R),
                ("max", Num, R),
                ("label", Str, O),
                ("decimals", Num, O),
            ],
        ),
        spec(
            "bind",
            Ctor,
            "std",
            "connect a parameter to an entity property or plot formula",
            &[
                ("parameter", Ident, R),
                ("target", Ident, R),
                ("property", Name, R),
                ("formula_or_from", Str, R),
                ("to", Num, O),
            ],
        ),
        spec(
            "cursor",
            Ctor,
            "std",
            "give a text entity a typewriter cursor",
            &[("id", Ident, R)],
        ),
        spec(
            "sticky",
            Ctor,
            "std",
            "pin an entity to the screen so it stays fixed through cam/zoom (HUD overlay)",
            &[("id", Ident, R)],
        ),
        spec("morph", Ctor, "std", "sampled-point shape morph a->b", &[]),
        spec(
            "copy",
            Ctor,
            "std",
            "duplicate an entity",
            &[("new", Name, R), ("src", Ident, R)],
        ),
        // ---- std: modifiers (t=0) ----
        spec(
            "color",
            Ctor,
            "std",
            "set fill/stroke colour",
            &[("id", Ident, R), ("color", Color, R)],
        ),
        spec(
            "outline",
            Ctor,
            "std",
            "set outline colour",
            &[("id", Ident, R), ("color", Color, R)],
        ),
        spec(
            "size",
            Ctor,
            "std",
            "set text size",
            &[("id", Ident, R), ("size", Num, R)],
        ),
        spec(
            "wrap",
            Ctor,
            "std",
            "wrap a text/caption/$…$ label to a width (px), breaking at word boundaries; inline math stays atomic",
            &[("id", Ident, R), ("width", Num, R)],
        ),
        spec(
            "stroke",
            Ctor,
            "std",
            "set stroke width",
            &[("id", Ident, R), ("width", Num, R)],
        ),
        spec(
            "dashed",
            Ctor,
            "std",
            "use a repeating dash/gap stroke on a path-like entity",
            &[
                ("id", Ident, R),
                ("dash", Num, O),
                ("gap", Num, O),
            ],
        ),
        spec(
            "glow",
            Ctor,
            "std",
            "set neon glow amount",
            &[("id", Ident, R), ("amount", Num, R)],
        ),
        spec(
            "opacity",
            Ctor,
            "std",
            "set opacity 0..1",
            &[("id", Ident, R), ("value", Num, R)],
        ),
        spec(
            "hue",
            Ctor,
            "std",
            "drive colour by an HSL hue",
            &[
                ("id", Ident, R),
                ("degrees", Num, R),
                ("s", Num, O),
                ("l", Num, O),
            ],
        ),
        spec(
            "rot",
            Ctor,
            "std",
            "set rotation (degrees)",
            &[("id", Ident, R), ("degrees", Num, R)],
        ),
        spec(
            "z",
            Ctor,
            "std",
            "set draw order",
            &[("id", Ident, R), ("z", Num, R)],
        ),
        spec(
            "tag",
            Ctor,
            "std",
            "add a group tag",
            &[("id", Ident, R), ("tag", Name, R)],
        ),
        spec(
            "bold",
            Ctor,
            "std",
            "use the bold mono font",
            &[("id", Ident, R)],
        ),
        spec("display", Ctor, "std", "mark visible", &[("id", Ident, R)]),
        spec(
            "hidden",
            Ctor,
            "std",
            "start hidden (opacity 0)",
            &[("id", Ident, R)],
        ),
        spec(
            "filled",
            Ctor,
            "std",
            "turn the fill on",
            &[("id", Ident, R)],
        ),
        spec(
            "outlined",
            Ctor,
            "std",
            "turn the outline on",
            &[("id", Ident, R)],
        ),
        spec(
            "untraced",
            Ctor,
            "std",
            "start undrawn, ready for draw-on",
            &[("id", Ident, R)],
        ),
        // ---- std: boolean ops ----
        spec("union", Ctor, "std", "boolean union of two shapes", &[]),
        spec("intersect", Ctor, "std", "boolean intersection", &[]),
        spec("intersection", Ctor, "std", "boolean intersection", &[]),
        spec("difference", Ctor, "std", "boolean difference a-b", &[]),
        spec("subtract", Ctor, "std", "boolean difference a-b", &[]),
        spec(
            "exclusion",
            Ctor,
            "std",
            "boolean symmetric difference",
            &[],
        ),
        spec("xor", Ctor, "std", "boolean symmetric difference", &[]),
        // ---- std: braces ----
        spec("brace", Ctor, "std", "a curly brace", &[]),
        spec("bracelabel", Ctor, "std", "a brace with a label", &[]),
        spec("bracetext", Ctor, "std", "a brace's text label", &[]),
        // ---- std: verbs ----
        spec(
            "draw",
            Verb,
            "std",
            "trace a stroke on",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "flow",
            Verb,
            "std",
            "send a luminous emphasis pulse over a path",
            &[("path", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "erase",
            Verb,
            "std",
            "reverse of draw",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "show",
            Verb,
            "std",
            "fade in",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "fade",
            Verb,
            "std",
            "fade out",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "flash",
            Verb,
            "std",
            "flash a colour, then restore",
            &[
                ("id", Ident, R),
                ("color", Color, O),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "recolor",
            Verb,
            "std",
            "permanently change colour",
            &[("id", Ident, R), ("color", Color, R), ("dur", Num, O)],
        ),
        spec(
            "pulse",
            Verb,
            "std",
            "grow-and-settle attention pulse",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "shake",
            Verb,
            "std",
            "horizontal shake (error gesture)",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "spin",
            Verb,
            "std",
            "spin about the centre",
            &[("id", Ident, R), ("degrees", Num, O), ("dur", Num, O)],
        ),
        spec(
            "move",
            Verb,
            "std",
            "move to an absolute point",
            &[
                ("id", Ident, R),
                ("to", Point, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "wander",
            Verb,
            "std",
            "gently move a particle group inside its source container",
            &[("particles", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "arrange",
            MutVerb,
            "std",
            "move persistent particles into a deterministic random, grid, or ring layout; random uses stable organic curved routes",
            &[
                ("particles", Ident, R),
                ("container", Ident, R),
                ("layout", Str, O),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "travel",
            MutVerb,
            "std",
            "move one persistent entity once along an existing path and stop at its endpoint",
            &[
                ("entity", Ident, R),
                ("path", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "attach",
            MutVerb,
            "std",
            "keep an entity pinned to another entity plus an optional offset; use target `none` to release",
            &[
                ("child", Ident, R),
                ("target", Ident, R),
                ("offset", Point, O),
            ],
        ),
        spec(
            "become",
            MutVerb,
            "std",
            "transform one entity into a declared visual blueprint while retaining the source id",
            &[
                ("source", Ident, R),
                ("target", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "turn",
            MutVerb,
            "std",
            "rotate one entity or tagged arrangement rigidly around a shared pivot",
            &[
                ("id_or_tag", Ident, R),
                ("pivot", Point, R),
                ("degrees", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "shift",
            Verb,
            "std",
            "move by a delta",
            &[
                ("id", Ident, R),
                ("by", Point, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "grow",
            Verb,
            "std",
            "animate a line/arrow endpoint",
            &[("id", Ident, R), ("to", Point, R), ("dur", Num, O)],
        ),
        spec(
            "scale",
            Verb,
            "std",
            "animate scale to a factor",
            &[("id", Ident, R), ("factor", Num, R), ("dur", Num, O)],
        ),
        spec(
            "rotate",
            Verb,
            "std",
            "animate rotation",
            &[
                ("id", Ident, R),
                ("degrees", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "say",
            Verb,
            "std",
            "crossfade text to new content",
            &[("id", Ident, R), ("text", Str, R), ("dur", Num, O)],
        ),
        spec(
            "rewrite",
            MutVerb,
            "std",
            "smoothly transform one authored LaTeX equation state into the next while matching unchanged visual parts; this animates supplied math and does not solve it",
            &[
                ("equation", Ident, R),
                ("latex", Str, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "type",
            Verb,
            "std",
            "typewriter reveal",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "to",
            Verb,
            "std",
            "animate any property to a value",
            &[
                ("id", Ident, R),
                ("prop", Name, R),
                ("value", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "set",
            Verb,
            "std",
            "alias of `to`",
            &[
                ("id", Ident, R),
                ("prop", Name, R),
                ("value", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "transform",
            Verb,
            "std",
            "apply a 2x2 matrix (ApplyMatrix)",
            &[
                ("id", Ident, R),
                ("origin", Point, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "swap",
            MutVerb,
            "std",
            "swap two entities, or array slots i,j",
            &[("a", Ident, R), ("b", Ident, R), ("dur", Num, O)],
        ),
        // variadic entity list + optional duration/arc/easing tail
        spec(
            "cycle",
            MutVerb,
            "std",
            "move entities cyclically into one another's positions along an optional arc",
            &[],
        ),
        spec(
            "cam",
            Verb,
            "std",
            "pan the camera to a point",
            &[("to", Point, R), ("dur", Num, O), ("ease", Ease, O)],
        ),
        spec(
            "zoom",
            Verb,
            "std",
            "zoom the camera",
            &[("factor", Num, R), ("dur", Num, O), ("ease", Ease, O)],
        ),
        spec(
            "karaoke",
            Verb,
            "std",
            "highlight caption words in sequence",
            &[],
        ),
        spec(
            "wordpop",
            Verb,
            "std",
            "pop caption words in one at a time",
            &[],
        ),
        // ---- math ----
        spec(
            "axes",
            Ctor,
            "math",
            "a coordinate frame (optional tick spacing in px)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("halfw", Num, R),
                ("halfh", Num, R),
                ("unit", Num, O),
            ],
        ),
        spec(
            "plot",
            Ctor,
            "math",
            "y = f(x) as a curve",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("sx", Num, R),
                ("sy", Num, R),
                ("formula", Fn, R),
                ("domain", Num, O),
            ],
        ),
        spec(
            "normal",
            Ctor,
            "math",
            "the normal (perpendicular) line to a curve at x",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("x", Num, R),
                ("len", Num, O),
            ],
        ),
        spec(
            "slope",
            Ctor,
            "math",
            "a live readout of a curve's slope at x",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("x", Num, R),
                ("offset", Point, O),
            ],
        ),
        spec(
            "area",
            Ctor,
            "math",
            "the filled region under a curve from a to b",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("a", Num, R),
                ("b", Num, R),
                ("n", Num, O),
            ],
        ),
        spec(
            "integral",
            Ctor,
            "math",
            "a live readout of the integral of a curve from a to b",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("a", Num, R),
                ("b", Num, R),
                ("at", Point, O),
            ],
        ),
        spec(
            "roots",
            Ctor,
            "math",
            "dots where a curve crosses zero",
            &[("id", Name, R), ("curve", Name, R), ("color", Color, O)],
        ),
        spec(
            "deriv",
            Ctor,
            "math",
            "the derivative f' of a curve, drawn as its own curve",
            &[("id", Name, R), ("curve", Name, R), ("color", Color, O)],
        ),
        spec(
            "accum",
            Ctor,
            "math",
            "the accumulation function integral(a..x) of a curve",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("a", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "extrema",
            Ctor,
            "math",
            "dots at a curve's maxima and minima (slope = 0)",
            &[("id", Name, R), ("curve", Name, R), ("color", Color, O)],
        ),
        spec(
            "inflections",
            Ctor,
            "math",
            "dots where a curve changes concavity (f'' = 0)",
            &[("id", Name, R), ("curve", Name, R), ("color", Color, O)],
        ),
        spec(
            "band",
            Ctor,
            "math",
            "the filled region between two curves",
            &[
                ("id", Name, R),
                ("top", Name, R),
                ("bottom", Name, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "taylor",
            Ctor,
            "math",
            "the degree-n Taylor polynomial of a curve about a",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("a", Num, R),
                ("n", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "limit",
            Ctor,
            "math",
            "visualise lim(x->a) f(x): the value approached, with an approaching dot",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("a", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "newton",
            Ctor,
            "math",
            "Newton's method zig-zag from a starting guess",
            &[
                ("id", Name, R),
                ("curve", Name, R),
                ("x0", Num, R),
                ("steps", Num, O),
            ],
        ),
        // variadic point list — leave params open (`&[]`) so any number of
        // points is accepted without a false "takes at most N" rejection
        spec(
            "spline",
            Ctor,
            "math",
            "a smooth curve through a list of points",
            &[],
        ),
        spec(
            "trajectory",
            Ctor,
            "math",
            "an ODE path: dx/dt, dy/dt integrated from a start point",
            &[
                ("id", Name, R),
                ("dx/dt", Str, R),
                ("dy/dt", Str, R),
                ("start", Point, R),
                ("center", Point, R),
                ("scale", Num, R),
                ("steps", Num, O),
            ],
        ),
        spec(
            "linmap",
            Ctor,
            "math",
            "a 2x2 matrix applied to the plane (grid + basis)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("span", Num, O),
            ],
        ),
        spec(
            "determinant",
            Ctor,
            "math",
            "the unit square mapped to a parallelogram; area = det",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "eigen",
            Ctor,
            "math",
            "a matrix's real eigenvector directions (with eigenvalues)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "diagonalise",
            Ctor,
            "math",
            "A = P D P^-1: in the eigenbasis the matrix is a pure stretch",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "diagonalize",
            Ctor,
            "math",
            "A = P D P^-1: in the eigenbasis the matrix is a pure stretch",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "rref",
            Ctor,
            "math",
            "animated Gaussian elimination to reduced row-echelon form",
            &[
                ("id", Name, R),
                ("data", Str, R),
                ("center", Point, R),
                ("cellw", Num, O),
                ("rowh", Num, O),
            ],
        ),
        spec(
            "project",
            Ctor,
            "math",
            "orthogonal projection of a vector onto a line (subspace)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("b", Point, R),
                ("a", Point, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "leastsquares",
            Ctor,
            "math",
            "the best-fit line through a point cloud (linear regression)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("data", Str, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "linsolve",
            Ctor,
            "math",
            "the row picture of Ax=b: two lines meeting at the solution",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("e", Num, R),
                ("f", Num, R),
                ("span", Num, O),
            ],
        ),
        spec(
            "span",
            Ctor,
            "math",
            "the span of one or two vectors (a line, or the whole plane)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("v", Point, R),
                ("w", Point, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "vector",
            Ctor,
            "math",
            "an arrow from an origin",
            &[
                ("id", Name, R),
                ("origin", Point, R),
                ("delta", Point, R),
                ("color", Color, O),
            ],
        ),
        spec("numberline", Ctor, "math", "a labelled number line", &[]),
        spec(
            "numberplane",
            Ctor,
            "math",
            "a gridded coordinate plane",
            &[],
        ),
        spec("plane", Ctor, "math", "a coordinate plane", &[]),
        spec("complexplane", Ctor, "math", "the complex plane", &[]),
        spec("polarplane", Ctor, "math", "a polar grid", &[]),
        spec(
            "matrix",
            Ctor,
            "math",
            "a bracketed matrix",
            &[
                ("id", Name, R),
                ("data", Str, R),
                ("center", Point, R),
                ("colw", Num, O),
                ("rowh", Num, O),
            ],
        ),
        spec(
            "table",
            Ctor,
            "math",
            "a ruled table",
            &[
                ("id", Name, R),
                ("data", Str, R),
                ("center", Point, R),
                ("colw", Num, O),
                ("rowh", Num, O),
                ("cols", Str, O),
                ("rows", Str, O),
            ],
        ),
        spec(
            "mathtable",
            Ctor,
            "math",
            "a table of math expressions",
            &[],
        ),
        spec("integertable", Ctor, "math", "a table of integers", &[]),
        spec("decimaltable", Ctor, "math", "a table of decimals", &[]),
        spec("arc", Ctor, "math", "a circular arc", &[]),
        spec("sector", Ctor, "math", "a pie sector", &[]),
        spec("annulus", Ctor, "math", "a ring / annular sector", &[]),
        spec("pie", Ctor, "math", "a pie chart", &[]),
        spec("arrowfield", Ctor, "math", "a named vector field", &[]),
        spec("vectorfield", Ctor, "math", "a vector field", &[]),
        // ---- map (experimental geographic border PoC) ----
        spec(
            "map",
            Ctor,
            "map",
            "a projected geographic viewport with explicit longitude/latitude bounds",
            &[("id", Name, R), ("center", Point, R), ("width", Num, R), ("height", Num, R), ("bounds", Str, R)],
        ),
        spec(
            "border",
            Ctor,
            "map",
            "a named geographic border projected into a map (PoC: IND)",
            &[("id", Name, R), ("map", Ident, R), ("country", Str, R)],
        ),
        // ---- geo ----
        spec(
            "point",
            Ctor,
            "geo",
            "a labelled point",
            &[("id", Name, R), ("at", Point, R), ("label", Str, O)],
        ),
        spec(
            "segment",
            Ctor,
            "geo",
            "segment between two points",
            &[("id", Name, R), ("a", Ident, R), ("b", Ident, R)],
        ),
        spec(
            "midpoint",
            Ctor,
            "geo",
            "midpoint of two points",
            &[("id", Name, R), ("a", Ident, R), ("b", Ident, R)],
        ),
        spec("centroid", Ctor, "geo", "centroid of a triangle", &[]),
        spec("circumcenter", Ctor, "geo", "circumcentre", &[]),
        spec("incenter", Ctor, "geo", "incentre", &[]),
        spec("orthocenter", Ctor, "geo", "orthocentre", &[]),
        spec("foot", Ctor, "geo", "foot of a perpendicular", &[]),
        spec("meet", Ctor, "geo", "line-line intersection", &[]),
        spec("linecircle", Ctor, "geo", "line-circle intersection", &[]),
        spec(
            "circlecircle",
            Ctor,
            "geo",
            "circle-circle intersection",
            &[],
        ),
        // overloaded: `tangent(id, curve, x, [len])` is the calculus tangent to
        // a plotted function; `tangent(id, p, c, thru)` is the circle
        // construction. Params left open (`&[]`) so neither form false-rejects.
        spec(
            "tangent",
            Ctor,
            "math",
            "tangent line to a curve at x (or tangent points to a circle)",
            &[],
        ),
        spec(
            "commontangent",
            Ctor,
            "geo",
            "a common tangent to two circles (each = centre + a point on it): segment {id} between the touch points {id}.a/{id}.b; type external/direct (default) or internal/transverse",
            &[("id", Name, R), ("oA", Ident, R), ("aOn", Ident, R), ("oB", Ident, R), ("bOn", Ident, R), ("type", Str, O)],
        ),
        spec("reflect", Ctor, "geo", "reflect a point over a line", &[]),
        spec("bisector", Ctor, "geo", "angle bisector", &[]),
        spec("rotpoint", Ctor, "geo", "rotate a point about another", &[]),
        spec("between", Ctor, "geo", "a point between two others", &[]),
        spec("anglepoint", Ctor, "geo", "a point at an angle", &[]),
        spec("circumcircle", Ctor, "geo", "circumscribed circle", &[]),
        spec("incircle", Ctor, "geo", "inscribed circle", &[]),
        spec(
            "circle2",
            Ctor,
            "geo",
            "circle from centre + a point on it",
            &[],
        ),
        spec("ellipse", Ctor, "geo", "an ellipse", &[]),
        spec("parabola", Ctor, "geo", "a parabola", &[]),
        spec("hyperbola", Ctor, "geo", "a hyperbola", &[]),
        spec(
            "fullline",
            Ctor,
            "geo",
            "an infinite line through two points",
            &[],
        ),
        spec("anglemark", Ctor, "geo", "an angle arc mark", &[]),
        spec("rightangle", Ctor, "geo", "a right-angle square mark", &[]),
        // ---- algo ----
        spec(
            "graph",
            Ctor,
            "algo",
            "a node/edge graph (weights via a-b:w)",
            &[
                ("id", Name, R),
                ("verts", Str, R),
                ("edges", Str, R),
                ("layout", Name, R),
                ("center", Point, R),
                ("scale", Num, R),
                ("radius", Num, O),
            ],
        ),
        spec(
            "array",
            Ctor,
            "algo",
            "a row of value cells in slot boxes",
            &[
                ("id", Name, R),
                ("vals", Str, R),
                ("center", Point, R),
                ("cw", Num, O),
                ("ch", Num, O),
            ],
        ),
        spec(
            "list",
            Ctor,
            "algo",
            "a linked list (singly/doubly/circular)",
            &[
                ("id", Name, R),
                ("vals", Str, R),
                ("center", Point, R),
                ("kind", Name, O),
                ("cw", Num, O),
                ("ch", Num, O),
            ],
        ),
        spec(
            "stack",
            Ctor,
            "algo",
            "a stack (LIFO, grows up)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("cw", Num, O),
                ("ch", Num, O),
            ],
        ),
        spec(
            "queue",
            Ctor,
            "algo",
            "a queue (FIFO, grows right)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("cw", Num, O),
                ("ch", Num, O),
            ],
        ),
        spec(
            "hashmap",
            Ctor,
            "algo",
            "n buckets with separate chaining",
            &[
                ("id", Name, R),
                ("n", Num, R),
                ("center", Point, R),
                ("ew", Num, O),
                ("ch", Num, O),
            ],
        ),
        spec(
            "pointer",
            Ctor,
            "algo",
            "an index caret under an array slot",
            &[
                ("id", Name, R),
                ("arr", Ident, R),
                ("slot", Num, R),
                ("label", Str, O),
            ],
        ),
        spec(
            "caret",
            Ctor,
            "algo",
            "a labelled triangle marker",
            &[
                ("id", Name, R),
                ("at", Point, R),
                ("label", Str, R),
                ("dir", Name, O),
            ],
        ),
        spec(
            "compare",
            Verb,
            "algo",
            "flash the values in two array slots",
            &[
                ("arr", Ident, R),
                ("i", Num, R),
                ("j", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "pointat",
            Verb,
            "algo",
            "slide an index pointer to a slot",
            &[
                ("id", Ident, R),
                ("arr", Ident, R),
                ("slot", Num, R),
                ("dur", Num, O),
            ],
        ),
        spec(
            "push",
            MutVerb,
            "algo",
            "push onto a stack",
            &[("id", Ident, R), ("value", Str, R), ("dur", Num, O)],
        ),
        spec(
            "pop",
            MutVerb,
            "algo",
            "pop the top of a stack",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "enqueue",
            MutVerb,
            "algo",
            "enqueue at the back",
            &[("id", Ident, R), ("value", Str, R), ("dur", Num, O)],
        ),
        spec(
            "dequeue",
            MutVerb,
            "algo",
            "dequeue from the front",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "insert",
            MutVerb,
            "algo",
            "splice a node into a list",
            &[
                ("id", Ident, R),
                ("after", Num, R),
                ("value", Str, R),
                ("dur", Num, O),
            ],
        ),
        spec(
            "remove",
            MutVerb,
            "algo",
            "unlink a list node",
            &[("id", Ident, R), ("index", Num, R), ("dur", Num, O)],
        ),
        spec(
            "put",
            MutVerb,
            "algo",
            "hash a key into a bucket + chain",
            &[
                ("id", Ident, R),
                ("key", Str, R),
                ("val", Str, R),
                ("dur", Num, O),
            ],
        ),
        spec(
            "get",
            Verb,
            "algo",
            "scan a bucket's chain for a key",
            &[("id", Ident, R), ("key", Str, R), ("dur", Num, O)],
        ),
        spec(
            "bfs",
            MutVerb,
            "algo",
            "breadth-first traversal (queue)",
            &[("g", Ident, R), ("start", Ident, R)],
        ),
        spec(
            "dfs",
            MutVerb,
            "algo",
            "depth-first traversal (stack)",
            &[("g", Ident, R), ("start", Ident, R)],
        ),
        spec(
            "dijkstra",
            MutVerb,
            "algo",
            "single-source shortest paths",
            &[("g", Ident, R), ("start", Ident, R)],
        ),
        // ---- three: 3D scene + camera ----
        spec(
            "camera3",
            Ctor,
            "three",
            "a Z-up orbit camera",
            &[
                ("eye", Point3, R),
                ("target", Point3, R),
                ("fov", Num, O),
                ("projection", Ident, O),
            ],
        ),
        spec(
            "point3",
            Ctor,
            "three",
            "a point in 3D space",
            &[("id", Name, R), ("at", Point3, R), ("radius", Num, O)],
        ),
        spec(
            "line3",
            Ctor,
            "three",
            "a 3D line segment",
            &[("id", Name, R), ("from", Point3, R), ("to", Point3, R)],
        ),
        spec(
            "arrow3",
            Ctor,
            "three",
            "a 3D vector arrow",
            &[("id", Name, R), ("from", Point3, R), ("to", Point3, R)],
        ),
        spec(
            "cube3",
            Ctor,
            "three",
            "a rotatable 3D cuboid",
            &[("id", Name, R), ("center", Point3, R), ("size", Point3, R)],
        ),
        spec(
            "linmap3",
            Ctor,
            "three",
            "a 3x3 matrix deforming the unit cube; volume = determinant",
            &[
                ("id", Name, R),
                ("center", Point3, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("e", Num, R),
                ("f", Num, R),
                ("g", Num, R),
                ("h", Num, R),
                ("i", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "eigen3",
            Ctor,
            "three",
            "the real eigenvector directions of a 3x3 matrix (invariant lines)",
            &[
                ("id", Name, R),
                ("center", Point3, R),
                ("a", Num, R),
                ("b", Num, R),
                ("c", Num, R),
                ("d", Num, R),
                ("e", Num, R),
                ("f", Num, R),
                ("g", Num, R),
                ("h", Num, R),
                ("i", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "sphere3",
            Ctor,
            "three",
            "a 3D sphere",
            &[("id", Name, R), ("center", Point3, R), ("radius", Num, R)],
        ),
        spec(
            "grid3",
            Ctor,
            "three",
            "an XY ground grid",
            &[
                ("id", Name, R),
                ("center", Point3, R),
                ("half", Num, R),
                ("spacing", Num, O),
            ],
        ),
        spec(
            "axes3",
            Ctor,
            "three",
            "three colored coordinate axes with ticks + numbers",
            &[
                ("id", Name, R),
                ("origin", Point3, R),
                ("length", Num, R),
                ("step", Num, O),
            ],
        ),
        spec(
            "move3",
            Verb,
            "three",
            "move a 3D entity",
            &[
                ("id", Ident, R),
                ("to", Point3, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "shift3",
            Verb,
            "three",
            "shift a 3D entity",
            &[
                ("id", Ident, R),
                ("by", Point3, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "rotate3",
            Verb,
            "three",
            "rotate to Euler angles",
            &[
                ("id", Ident, R),
                ("degrees", Point3, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "grow3",
            Verb,
            "three",
            "retarget a 3D line or arrow",
            &[
                ("id", Ident, R),
                ("to", Point3, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "orbit3",
            Verb,
            "three",
            "orbit the 3D camera",
            &[
                ("azimuth", Num, R),
                ("elevation", Num, R),
                ("radius", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "roll3",
            Verb,
            "three",
            "roll the 3D camera around its viewing direction",
            &[("degrees", Num, R), ("dur", Num, O), ("ease", Ease, O)],
        ),
        spec(
            "look3",
            Verb,
            "three",
            "move the 3D camera target",
            &[("target", Point3, R), ("dur", Num, O), ("ease", Ease, O)],
        ),
        spec(
            "view3",
            MutVerb,
            "three",
            "smoothly frame a 3D entity or tagged group from a named view",
            &[
                ("target", Ident, R),
                ("view", Str, R),
                ("dur", Num, O),
                ("ease", Ease, O),
                ("margin", Num, O),
            ],
        ),
        spec(
            "travel3",
            MutVerb,
            "three",
            "move a persistent 3D entity along a line, arrow, or curve",
            &[
                ("entity", Ident, R),
                ("path", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "attach3",
            MutVerb,
            "three",
            "attach one 3D entity to another, or release it with none",
            &[
                ("child", Ident, R),
                ("target", Ident, R),
                ("offset", Point3, O),
                ("mode", Ident, O),
            ],
        ),
        spec(
            "become3",
            MutVerb,
            "three",
            "transform a persistent 3D entity into a blueprint",
            &[
                ("source", Ident, R),
                ("blueprint", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "turn3",
            MutVerb,
            "three",
            "turn a 3D entity or tagged group around a world-space pivot and axis",
            &[
                ("target", Ident, R),
                ("pivot", Point3, R),
                ("axis", Ident, R),
                ("degrees", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "pin3",
            Ctor,
            "three",
            "glue a 2D label to a 3D point",
            &[("label", Ident, R), ("at", Point3, R)],
        ),
        spec(
            "follow3",
            Ctor,
            "three",
            "track another 3D entity + offset each frame",
            &[("id", Ident, R), ("target", Ident, R), ("offset", Point3, O)],
        ),
        spec(
            "midpoint3",
            Ctor,
            "three",
            "a point at the midpoint of two 3D entities",
            &[("id", Name, R), ("a", Ident, R), ("b", Ident, R)],
        ),
        spec(
            "link3",
            Ctor,
            "three",
            "a live 3D edge between two moving entities",
            &[("id", Name, R), ("a", Ident, R), ("b", Ident, R), ("trim", Num, O)],
        ),
        spec(
            "project3",
            Ctor,
            "three",
            "live orthogonal projection of a 3D entity onto xy, xz, or yz",
            &[("id", Name, R), ("source", Ident, R), ("plane", Str, R)],
        ),
        spec(
            "contour3",
            Ctor,
            "three",
            "a level curve on a surface3 height field",
            &[("id", Name, R), ("surface", Ident, R), ("level", Num, R)],
        ),
        spec(
            "label3",
            Ctor,
            "three",
            "projected label with optional world-space height",
            &[("label", Ident, R), ("target", Ident, R), ("world_height", Num, O)],
        ),
        spec(
            "curve3",
            Ctor,
            "three",
            "parametric 3D curve from x(t)/y(t)/z(t) formulas",
            &[
                ("id", Name, R),
                ("x", Str, R),
                ("y", Str, R),
                ("z", Str, R),
                ("domain", Point, O),
            ],
        ),
        spec(
            "surface3",
            Ctor,
            "three",
            "filled, flat-shaded height-field surface z = f(x,y)",
            &[
                ("id", Name, R),
                ("z", Str, R),
                ("xdom", Point, R),
                ("ydom", Point, R),
                ("res", Num, O),
            ],
        ),
        spec(
            "gradient3",
            Ctor,
            "three",
            "steepest-ascent arrow on a surface3 at (x,y)",
            &[
                ("id", Name, R),
                ("surface", Name, R),
                ("x", Num, R),
                ("y", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "tangentplane3",
            Ctor,
            "three",
            "the plane tangent to a surface3 at (x,y)",
            &[
                ("id", Name, R),
                ("surface", Name, R),
                ("x", Num, R),
                ("y", Num, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "volume3",
            Ctor,
            "three",
            "the volume under a surface3 as a grid of columns",
            &[
                ("id", Name, R),
                ("surface", Name, R),
                ("res", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "param3",
            Ctor,
            "three",
            "parametric surface x(u,v),y(u,v),z(u,v) — tori, shells, Möbius",
            &[
                ("id", Name, R),
                ("x", Str, R),
                ("y", Str, R),
                ("z", Str, R),
                ("udom", Point, R),
                ("vdom", Point, R),
                ("res", Num, O),
            ],
        ),
        spec(
            "prism3",
            Ctor,
            "three",
            "regular n-gon prism (many sides ≈ cylinder)",
            &[
                ("id", Name, R),
                ("center", Point3, R),
                ("sides", Num, R),
                ("radius", Num, R),
                ("height", Num, R),
            ],
        ),
        spec(
            "pyramid3",
            Ctor,
            "three",
            "regular n-gon pyramid (many sides ≈ cone)",
            &[
                ("id", Name, R),
                ("center", Point3, R),
                ("sides", Num, R),
                ("radius", Num, R),
                ("height", Num, R),
            ],
        ),
        spec(
            "revolve3",
            Ctor,
            "three",
            "solid of revolution from a radius profile r(t)",
            &[
                ("id", Name, R),
                ("center", Point3, R),
                ("profile", Str, R),
                ("tdom", Point, R),
                ("sides", Num, O),
            ],
        ),
        spec(
            "extrude3",
            Ctor,
            "three",
            "extrude a 2D shape or boolean region into a solid prism (CSG)",
            &[
                ("id", Name, R),
                ("source", Ident, R),
                ("height", Num, R),
                ("center", Point3, O),
            ],
        ),
        spec(
            "morph3",
            Ctor,
            "three",
            "set 3D entity a up to morph into b's shape (animate with `to(a, morph, 1, dur)`)",
            &[("a", Ident, R), ("b", Ident, R), ("spin", Num, O)],
        ),
        spec(
            "thick",
            Ctor,
            "three",
            "render a 3D curve3/line3/arrow3 as a tube of the given world radius",
            &[("id", Ident, R), ("radius", Num, R)],
        ),
        spec(
            "finish3",
            Ctor,
            "three",
            "bounded 3D shading, material, texture, mesh, depth, and shadow finish",
            &[("id", Ident, R), ("spec", Str, R)],
        ),
        spec(
            "model3",
            Ctor,
            "three",
            "load deterministic geometry from a bundled asset: URI or safety-limited OBJ path",
            &[("id", Name, R), ("path", Str, R), ("center", Point3, R), ("scale", Num, O)],
        ),
        spec(
            "tube3",
            Ctor,
            "three",
            "variable-radius tube around a line3, arrow3, or curve3",
            &[("id", Name, R), ("path", Ident, R), ("radius", Str, R), ("sides", Num, O)],
        ),
        // ---- brand ----
        spec(
            "banner",
            Ctor,
            "brand",
            "the manic logo/banner",
            &[("id", Name, R), ("center", Point, R), ("scale", Num, O)],
        ),
        spec(
            "watermark",
            Ctor,
            "brand",
            "a screen-fixed watermark; omit the point for responsive bottom-right placement or pass an exact point",
            &[("id", Name, R), ("at", Point, O), ("text", Str, O)],
        ),
        spec(
            "histogram",
            Ctor,
            "stats",
            "bin a dataset into bars — the shape of the data",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("data", Str, R),
                ("bins", Num, O),
                ("width", Num, O),
                ("height", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "summary",
            Ctor,
            "stats",
            "describe a dataset: mean/median/mode markers, spread, and readouts",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("data", Str, R),
                ("width", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "correlation",
            Ctor,
            "stats",
            "scatter + best-fit line + the Pearson correlation r",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("data", Str, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "skew",
            Ctor,
            "stats",
            "a histogram labelled with its skewness (left / right / symmetric)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("data", Str, R),
                ("bins", Num, O),
                ("width", Num, O),
                ("height", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "boxplot",
            Ctor,
            "stats",
            "five-number summary as a box-and-whisker (box = IQR)",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("data", Str, R),
                ("width", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "hypothesis",
            Ctor,
            "stats",
            "a two-tailed significance test: p-value as shaded normal tails",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("z", Num, R),
                ("alpha", Num, O),
                ("unit", Num, O),
            ],
        ),
        spec(
            "covariance",
            Ctor,
            "stats",
            "covariance as signed-area rectangles about the mean cross",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("unit", Num, R),
                ("data", Str, R),
                ("color", Color, O),
            ],
        ),
        spec(
            "bayes",
            Ctor,
            "stats",
            "Bayesian updating: prior + likelihood -> posterior for a coin's bias",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("heads", Num, R),
                ("tails", Num, R),
                ("width", Num, O),
                ("height", Num, O),
            ],
        ),
        spec(
            "distribution",
            Ctor,
            "stats",
            "a named distribution: uniform / exponential / binomial / poisson",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("kind", Str, R),
                ("a", Num, R),
                ("b", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "confidence",
            Ctor,
            "stats",
            "a confidence interval for a mean: estimate + z*sd/sqrt(n) error bar",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("mean", Num, R),
                ("sd", Num, R),
                ("n", Num, R),
                ("level", Num, O),
                ("width", Num, O),
            ],
        ),
        spec(
            "montecarlo",
            Ctor,
            "stats",
            "estimate pi by darts: random points in a square vs the circle",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("points", Num, R),
                ("seed", Num, O),
                ("size", Num, O),
            ],
        ),
        spec(
            "randomwalk",
            Ctor,
            "stats",
            "a 2D random walk path from the centre",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("steps", Num, R),
                ("seed", Num, O),
                ("scale", Num, O),
            ],
        ),
        spec(
            "bellcurve",
            Ctor,
            "stats",
            "the normal/Gaussian bell curve with the 68-95-99.7 rule shaded",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("mu", Num, R),
                ("sigma", Num, R),
                ("unit", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "gaussian",
            Ctor,
            "stats",
            "the normal/Gaussian bell curve with the 68-95-99.7 rule shaded",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("mu", Num, R),
                ("sigma", Num, R),
                ("unit", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "lln",
            Ctor,
            "stats",
            "Law of Large Numbers: a running proportion settling onto the truth",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("trials", Num, R),
                ("seed", Num, O),
                ("width", Num, O),
                ("height", Num, O),
            ],
        ),
        spec(
            "clt",
            Ctor,
            "stats",
            "Central Limit Theorem: sample means of dice pile into a bell",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("samplesize", Num, R),
                ("trials", Num, R),
                ("seed", Num, O),
                ("width", Num, O),
                ("height", Num, O),
                ("color", Color, O),
            ],
        ),
        // ── physics ──
        spec(
            "pendulum",
            Ctor,
            "physics",
            "a swinging pendulum (pre-simulated); animate with swing(id)",
            &[
                ("id", Name, R),
                ("center", Point, O),
                ("length", Num, O),
                ("angle0", Num, O),
                ("unit", Num, O),
                ("damping", Num, O),
            ],
        ),
        spec(
            "spring",
            Ctor,
            "physics",
            "a mass on a spring (pre-simulated); animate with run(id)",
            &[
                ("id", Name, R),
                ("center", Point, O),
                ("stiffness", Num, O),
                ("x0", Num, O),
                ("unit", Num, O),
                ("damping", Num, O),
            ],
        ),
        spec(
            "doublependulum",
            Ctor,
            "physics",
            "the chaotic double pendulum (pre-simulated); animate with run(id)",
            &[
                ("id", Name, R),
                ("center", Point, O),
                ("angle1", Num, O),
                ("angle2", Num, O),
                ("unit", Num, O),
            ],
        ),
        spec(
            "springpendulum",
            Ctor,
            "physics",
            "an elastic pendulum (swings + bounces); animate with run(id)",
            &[("id", Name, R), ("center", Point, O), ("angle0", Num, O), ("stretch0", Num, O), ("unit", Num, O), ("damping", Num, O)],
        ),
        spec(
            "kapitza",
            Ctor,
            "physics",
            "a Kapitza pendulum — fast vibration stabilises the inverted position",
            &[("id", Name, R), ("center", Point, O), ("angle0", Num, O), ("vibeamp", Num, O), ("unit", Num, O)],
        ),
        spec(
            "cartpendulum",
            Ctor,
            "physics",
            "a pendulum on a spring-mounted cart (control-theory system)",
            &[("id", Name, R), ("center", Point, O), ("angle0", Num, O), ("unit", Num, O)],
        ),
        spec(
            "comparependulum",
            Ctor,
            "physics",
            "two chaotic pendulums a hair apart — sensitive dependence",
            &[("id", Name, R), ("center", Point, O), ("angle0", Num, O), ("unit", Num, O)],
        ),
        spec(
            "verticalspring",
            Ctor,
            "physics",
            "a mass bobbing on a vertical spring under gravity",
            &[("id", Name, R), ("center", Point, O), ("stretch0", Num, O), ("unit", Num, O), ("damping", Num, O)],
        ),
        spec(
            "springincline",
            Ctor,
            "physics",
            "a mass on a spring on an inclined plane",
            &[("id", Name, R), ("center", Point, O), ("angle", Num, O), ("unit", Num, O), ("damping", Num, O)],
        ),
        spec(
            "bungee",
            Ctor,
            "physics",
            "a bungee jump — free-fall then a one-sided elastic cord",
            &[("id", Name, R), ("center", Point, O), ("unit", Num, O), ("damping", Num, O)],
        ),
        spec(
            "resonance",
            Ctor,
            "physics",
            "a driven spring — amplitude peaks near the natural frequency",
            &[("id", Name, R), ("center", Point, O), ("drivefreq", Num, O), ("unit", Num, O), ("damping", Num, O)],
        ),
        spec(
            "doublespring",
            Ctor,
            "physics",
            "two masses coupled by springs — beating / normal modes",
            &[("id", Name, R), ("center", Point, O), ("unit", Num, O)],
        ),
        spec(
            "seriesparallel",
            Ctor,
            "physics",
            "springs in series vs parallel — soft/slow vs stiff/fast",
            &[("id", Name, R), ("center", Point, O), ("unit", Num, O)],
        ),
        spec(
            "carsuspension",
            Ctor,
            "physics",
            "a quarter-car riding a scrolling road (bump/washboard/pothole)",
            &[("id", Name, R), ("center", Point, O), ("unit", Num, O)],
        ),
        spec(
            "piston",
            Ctor,
            "physics",
            "an engine piston — a spinning crank drives a slider in a cylinder",
            &[("id", Name, R), ("center", Point, O), ("rpm", Num, O), ("unit", Num, O)],
        ),
        spec(
            "molecule",
            Ctor,
            "physics",
            "N atoms bonded by springs, vibrating about their shape",
            &[("id", Name, R), ("center", Point, O), ("atoms", Num, O), ("unit", Num, O)],
        ),
        spec(
            "robotarm",
            Ctor,
            "physics",
            "a two-link robot arm tracking a target by inverse kinematics (mode 1=circle, 2=figure-8, 0=fixed)",
            &[("id", Name, R), ("center", Point, O), ("mode", Num, O), ("unit", Num, O)],
        ),
        spec(
            "pulley",
            Ctor,
            "physics",
            "a vertical Atwood machine — two masses over one pulley",
            &[("id", Name, R), ("center", Point, O), ("m1", Num, O), ("m2", Num, O), ("unit", Num, O)],
        ),
        spec(
            "pulleyscale",
            Ctor,
            "physics",
            "an Atwood machine with an in-line spring scale reading the rope tension",
            &[("id", Name, R), ("center", Point, O), ("m1", Num, O), ("m2", Num, O), ("unit", Num, O)],
        ),
        spec(
            "blocktackle",
            Ctor,
            "physics",
            "a compound pulley (block & tackle): N strands give a mechanical advantage of N",
            &[("id", Name, R), ("center", Point, O), ("load", Num, O), ("effort", Num, O), ("strands", Num, O), ("unit", Num, O)],
        ),
        spec(
            "compoundpulley",
            Ctor,
            "physics",
            "a compound pulley: a fixed pulley + a movable pulley with three masses A, B, C (static when mA = mB+mC)",
            &[("id", Name, R), ("center", Point, O), ("mA", Num, O), ("mB", Num, O), ("mC", Num, O), ("unit", Num, O)],
        ),
        spec(
            "ramp",
            Ctor,
            "physics",
            "a block sliding on an inclined plane with friction",
            &[("id", Name, R), ("center", Point, O), ("angle", Num, O), ("mass", Num, O), ("applied", Num, O), ("unit", Num, O)],
        ),
        spec(
            "dropmass",
            Ctor,
            "physics",
            "a mass dropped onto a spring-block — inelastic collision, energy lost",
            &[("id", Name, R), ("center", Point, O), ("dropheight", Num, O), ("unit", Num, O)],
        ),
        spec(
            "inclinepulley",
            Ctor,
            "physics",
            "a block on an incline tied over a top pulley to a hanging mass (incline-Atwood)",
            &[("id", Name, R), ("center", Point, O), ("angle", Num, O), ("m1", Num, O), ("m2", Num, O), ("unit", Num, O)],
        ),
        spec(
            "doubleincline",
            Ctor,
            "physics",
            "two blocks on a wedge's two slopes, tied over an apex pulley (right slope rough)",
            &[("id", Name, R), ("center", Point, O), ("angle1", Num, O), ("angle2", Num, O), ("m1", Num, O), ("m2", Num, O), ("unit", Num, O)],
        ),
        spec(
            "inclinebumper",
            Ctor,
            "physics",
            "a block slides down an incline into a spring bumper at the base, then bounces back",
            &[("id", Name, R), ("center", Point, O), ("angle", Num, O), ("mass", Num, O), ("stiffness", Num, O), ("unit", Num, O)],
        ),
        spec(
            "springchain",
            Ctor,
            "physics",
            "three blocks joined by two springs on an incline — coupled oscillators / normal modes",
            &[("id", Name, R), ("center", Point, O), ("angle", Num, O), ("unit", Num, O)],
        ),
        spec(
            "looptrack",
            Ctor,
            "physics",
            "a ball rolls down a ramp and around a vertical loop-the-loop (curved track)",
            &[("id", Name, R), ("center", Point, O), ("radius", Num, O), ("height", Num, O), ("unit", Num, O)],
        ),
        spec(
            "collideblocks",
            Ctor,
            "physics",
            "two blocks collide on a frictionless track with elastic walls (restitution e)",
            &[("id", Name, R), ("center", Point, O), ("m1", Num, O), ("m2", Num, O), ("restitution", Num, O), ("unit", Num, O)],
        ),
        spec(
            "bulletblock",
            Ctor,
            "physics",
            "a bullet fired into a block embeds (perfectly inelastic — most kinetic energy lost)",
            &[("id", Name, R), ("center", Point, O), ("bulletmass", Num, O), ("speed", Num, O), ("blockmass", Num, O), ("unit", Num, O)],
        ),
        spec(
            "newtonscradle",
            Ctor,
            "physics",
            "Newton's cradle — pull N balls, N swing out (event-driven elastic collisions)",
            &[("id", Name, R), ("center", Point, O), ("balls", Num, O), ("pulled", Num, O)],
        ),
        spec(
            "stringwave",
            Ctor,
            "physics",
            "a wave on a plucked string (N masses on springs, fixed ends) — the wave equation",
            &[("id", Name, R), ("center", Point, O), ("width", Num, O), ("amp", Num, O), ("pluck", Num, O)],
        ),
        spec(
            "raft",
            Ctor,
            "physics",
            "a person walking on a floating raft — centre of mass stays fixed",
            &[("id", Name, R), ("center", Point, O), ("personmass", Num, O), ("raftmass", Num, O), ("unit", Num, O)],
        ),
        spec(
            "brachistochrone",
            Ctor,
            "physics",
            "four beads race down different curves under gravity — the cycloid wins",
            &[("id", Name, R), ("center", Point, O), ("unit", Num, O)],
        ),
        spec(
            "run",
            Verb,
            "physics",
            "replay a sim's motion over [dur] seconds (alias: swing)",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "swing",
            Verb,
            "physics",
            "replay a pendulum's motion over [dur] seconds (alias of run)",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "forces",
            Verb,
            "physics",
            "reveal a sim's free-body force diagram (ramp: gravity, normal, friction, a)",
            &[("id", Ident, R), ("dur", Num, O)],
        ),
        spec(
            "phase",
            Ctor,
            "physics",
            "a sim's phase portrait (e.g. θ vs ω); animates with swing",
            &[("id", Ident, R), ("center", Point, R), ("size", Num, O)],
        ),
        spec(
            "well",
            Ctor,
            "physics",
            "a sim's potential-energy well with the body as a rolling ball",
            &[("id", Ident, R), ("center", Point, R), ("size", Num, O)],
        ),
        spec(
            "timegraph",
            Ctor,
            "physics",
            "a sim's phase variables as curves over time (with a sweep line)",
            &[("id", Ident, R), ("center", Point, R), ("size", Num, O)],
        ),
        spec(
            "energygraph",
            Ctor,
            "physics",
            "KE/PE/total energy as curves over time (with a sweep line)",
            &[("id", Ident, R), ("center", Point, R), ("size", Num, O)],
        ),
        // ---- ml: truthful model computation + progressive visual focus ----
        spec(
            "network",
            Ctor,
            "ml",
            "a deterministic feed-forward neural network with responsive layered layout and stable node/edge tags",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("layers", Str, R),
                ("activations", Str, R),
                ("width", Num, O),
                ("height", Num, O),
                ("seed", Num, O),
            ],
        ),
        spec(
            "activation",
            Ctor,
            "ml",
            "a truthful plot of linear, ReLU, sigmoid, or tanh",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("function", Ident, R),
                ("width", Num, O),
                ("height", Num, O),
            ],
        ),
        spec(
            "forward",
            MutVerb,
            "ml",
            "compute and progressively reveal one feed-forward pass with real activations and output values",
            &[
                ("network", Ident, R),
                ("input", Str, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "loss",
            MutVerb,
            "ml",
            "compare the latest network output with a target using cross-entropy or mean-squared error",
            &[
                ("network", Ident, R),
                ("target", Str, R),
                ("kind", Ident, O),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "backward",
            MutVerb,
            "ml",
            "compute exact reverse-mode gradients and reveal them from output to input",
            &[
                ("network", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "update",
            MutVerb,
            "ml",
            "apply one gradient-descent parameter update and recompute the prediction and loss",
            &[
                ("network", Ident, R),
                ("learning_rate", Num, O),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "checkpoint",
            MutVerb,
            "ml",
            "save one network's exact parameters, prediction, target, and loss for a later rollback",
            &[("id", Name, R), ("network", Ident, R)],
        ),
        spec(
            "restore",
            MutVerb,
            "ml",
            "animate and exactly restore a checkpointed network state; this is rollback, not dataset-level unlearning",
            &[
                ("network", Ident, R),
                ("checkpoint", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "tensor",
            Ctor,
            "ml",
            "a finite numeric grid; semicolons separate rows and vertical bars separate channels",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("values", Str, R),
                ("cell", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "kernel",
            Ctor,
            "ml",
            "a finite convolution kernel with one grid per input channel",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("values", Str, R),
                ("cell", Num, O),
                ("color", Color, O),
            ],
        ),
        spec(
            "convolve",
            Ctor,
            "ml",
            "compute a feature map from an input tensor and kernel with validated stride, padding, bias, and activation",
            &[
                ("id", Name, R),
                ("input", Ident, R),
                ("kernel", Ident, R),
                ("center", Point, R),
                ("stride", Num, O),
                ("padding", Num, O),
                ("bias", Num, O),
                ("activation", Ident, O),
                ("cell", Num, O),
            ],
        ),
        spec(
            "pool",
            Ctor,
            "ml",
            "compute deterministic max or average pooling independently across tensor channels",
            &[
                ("id", Name, R),
                ("input", Ident, R),
                ("center", Point, R),
                ("kind", Ident, R),
                ("window", Num, O),
                ("stride", Num, O),
                ("padding", Num, O),
                ("cell", Num, O),
            ],
        ),
        spec(
            "scan",
            MutVerb,
            "ml",
            "coordinate the receptive field, operator, arithmetic, and destination cell of a convolution or pool",
            &[
                ("output", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "tokenize",
            Ctor,
            "ml",
            "split text into a small deterministic token sequence using authored, word, or character boundaries",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("text", Str, R),
                ("mode", Ident, O),
                ("width", Num, O),
            ],
        ),
        spec(
            "embedding",
            Ctor,
            "ml",
            "turn a token sequence into explicit or seeded educational vectors and add sinusoidal or no positional encoding",
            &[
                ("id", Name, R),
                ("tokens", Ident, R),
                ("center", Point, R),
                ("vectors", Str, R),
                ("position", Ident, O),
                ("width", Num, O),
                ("height", Num, O),
            ],
        ),
        spec(
            "transformer",
            Ctor,
            "ml",
            "compute and draw a complete deterministic transformer block from an ML5 embedding and a compact heads/mask/MLP/norm/dropout specification",
            &[
                ("id", Name, R),
                ("embedding", Ident, R),
                ("center", Point, R),
                ("config", Str, R),
                ("width", Num, O),
                ("height", Num, O),
            ],
        ),
        spec(
            "encode",
            MutVerb,
            "ml",
            "reveal a transformer block through heads, masking, concatenation, residuals, normalization, MLP, dropout, and output",
            &[
                ("transformer", Ident, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "logits",
            Ctor,
            "ml",
            "project one transformer hidden row into an authored educational vocabulary and compute the full temperature-scaled softmax",
            &[
                ("id", Name, R),
                ("transformer", Ident, R),
                ("token", Num, R),
                ("center", Point, R),
                ("labels", Str, R),
                ("temperature", Num, O),
                ("width", Num, O),
                ("height", Num, O),
                ("seed", Num, O),
            ],
        ),
        spec(
            "sample",
            MutVerb,
            "ml",
            "filter, renormalize, and deterministically select from a logits view using greedy, categorical, top-k, or top-p decoding",
            &[
                ("logits", Ident, R),
                ("strategy", Str, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "attention",
            Ctor,
            "ml",
            "compute and draw one deterministic scaled dot-product self-attention head from explicit token embeddings",
            &[
                ("id", Name, R),
                ("center", Point, R),
                ("tokens", Str, R),
                ("embeddings", Str, R),
                ("width", Num, O),
                ("height", Num, O),
                ("seed", Num, O),
            ],
        ),
        spec(
            "attend",
            MutVerb,
            "ml",
            "focus one 1-based query token and reveal its Q/K matches, softmax row, weighted V mix, and residual lane",
            &[
                ("attention", Ident, R),
                ("token", Num, R),
                ("dur", Num, O),
                ("ease", Ease, O),
            ],
        ),
        spec(
            "topk",
            Ctor,
            "ml",
            "project one attention residual into an authored candidate vocabulary and draw the exact top-k softmax probabilities",
            &[
                ("id", Name, R),
                ("attention", Ident, R),
                ("token", Num, R),
                ("center", Point, R),
                ("labels", Str, R),
                ("k", Num, O),
                ("width", Num, O),
                ("height", Num, O),
                ("seed", Num, O),
            ],
        ),
        // ---- optics: light as geometry (real Snell + dispersion) ----
        spec(
            "refract",
            Ctor,
            "optics",
            "a light ray bending across a boundary (Snell's law); run(id) sweeps the angle, showing total internal reflection",
            &[("id", Name, R), ("center", Point, O), ("n1", Num, O), ("n2", Num, O), ("angle", Num, O)],
        ),
        spec(
            "lens",
            Ctor,
            "optics",
            "a converging lens focusing parallel rays to a focal point; run(id) sweeps the focal length so the focus slides",
            &[("id", Name, R), ("center", Point, O), ("focal", Num, O), ("aperture", Num, O)],
        ),
        spec(
            "prism",
            Ctor,
            "optics",
            "white light splitting into a spectrum through a prism (real Sellmeier dispersion); run(id) sweeps the incidence angle",
            &[("id", Name, R), ("center", Point, O), ("glass", Str, O)],
        ),
        spec(
            "achromat",
            Ctor,
            "optics",
            "chromatic aberration (red & blue focus apart) and its fix; run(id) sweeps in the achromatic doublet, merging the foci",
            &[("id", Name, R), ("center", Point, O), ("aperture", Num, O)],
        ),
        spec(
            "lenssystem",
            Ctor,
            "optics",
            "a real multi-element lens ray-traced through its spherical/aspheric surfaces (named preset OR a custom prescription string); draw(id.rays) sketches rays, run(id) sweeps a focus sensor + spot read-out. Optional finite object distance (px).",
            &[("id", Name, R), ("center", Point, O), ("preset", Str, O), ("object", Num, O)],
        ),
        spec(
            "rayfan",
            Ctor,
            "optics",
            "the ray-fan aberration plot of a lens preset (transverse ray error vs pupil height); draw(id.curve) sketches the spherical-aberration S-curve",
            &[("id", Name, R), ("center", Point, O), ("preset", Str, O)],
        ),
        spec(
            "spotdiagram",
            Ctor,
            "optics",
            "the spot diagram of a lens preset at best focus (rays land in a blur disc — spherical aberration), with an RMS read-out and the ideal point focus",
            &[("id", Name, R), ("center", Point, O), ("preset", Str, O)],
        ),
        spec(
            "fieldspot",
            Ctor,
            "optics",
            "the off-axis spot diagram (3-D pupil trace at a field angle) — a coma comet / astigmatic blur off-axis, with an Airy-disk diffraction-limit overlay",
            &[("id", Name, R), ("center", Point, O), ("preset", Str, O), ("field", Num, O)],
        ),
        // ---- creator: social-video format templates ----
        spec(
            "creator",
            Ctor,
            "creator",
            "a reusable v2 creator profile: handle/platforms plus name, tagline, logo, palette, footer, CTA and safe-area keys",
            &[("id", Name, R), ("spec", Str, R)],
        ),
        spec(
            "socials",
            Ctor,
            "creator",
            "draw the creator profile's responsive footer; social mode uses normalized native YouTube/X/Instagram/TikTok/Facebook/LinkedIn/GitHub/web/email icons",
            &[("id", Ident, R), ("at", Point, O)],
        ),
        spec(
            "quiz",
            Ctor,
            "creator",
            "start a responsive quiz: studio default; optional skin/reveal/layout/density/labels/timer/pace/seconds/motion/safe/accent controls",
            &[("id", Name, R), ("question", Str, R), ("style", Str, O)],
        ),
        spec(
            "option",
            Ctor,
            "creator",
            "add one of up to six fitted answer cards with stable A–F semantic tags; a trailing `correct` marks the right one",
            &[("id", Ident, R), ("text", Str, R), ("correct", Ident, O)],
        ),
        spec(
            "timing",
            Ctor,
            "creator",
            "generic named-phase timing controller (optional position) or quiz-specific pace/phase configuration; use generic controllers with timed/during",
            &[],
        ),
        spec(
            "timerstyle",
            Ctor,
            "creator",
            "style a quiz or generic timing controller: optional generic position plus native look, number, direction, size, colours, label and finish",
            &[],
        ),
        spec(
            "safezone",
            Ctor,
            "creator",
            "a responsive safe-area guide; optional numeric inset or shorts/reels/tiktok/clean profile",
            &[("id", Name, R), ("inset", Num, O)],
        ),
        spec(
            "countdown",
            Ctor,
            "creator",
            "a standalone Timing v2 widget sharing the quiz timer looks and style controls; play with run(id, secs)",
            &[("id", Name, R), ("at", Point, O), ("secs", Num, O), ("style", Str, O)],
        ),
        spec(
            "figure",
            Ctor,
            "creator",
            "fit a complete tagged group into the responsive media region, including text/images/equations and live-dependency checks",
            &[("target", Ident, R), ("center", Point, O), ("size", Point, O)],
        ),
        spec(
            "explain",
            Ctor,
            "creator",
            "attach optional author-supplied answer context and source to a quiz reveal",
            &[("quiz", Ident, R), ("text", Str, R), ("source", Str, O)],
        ),
        spec(
            "endcard",
            Ctor,
            "creator",
            "build a hidden responsive creator end card; reveal profile.endcard at the final beat",
            &[("profile", Ident, R), ("spec", Str, O)],
        ),
    ]
}

/// Palette colour names (the only colours the DSL accepts).
pub const COLORS: &[&str] = &[
    "fg", "void", "cyan", "magenta", "lime", "gold", "red", "orange", "blue", "dim", "panel",
    "rainbow",
];

/// Easing names accepted where a `[ease]` argument is allowed.
pub const EASINGS: &[&str] = &[
    "linear",
    "smooth",
    "inout",
    "in",
    "out",
    "overshoot",
    "back",
    "bounce",
    "elastic",
    "spring",
];

/// Named plot functions accepted where a `Fn` argument is allowed (`plot`'s
/// function slot) — a bareword alternative to a `"formula"` string. MUST stay in
/// sync with the engine's `named_formula` table; guarded by the engine test
/// `catalog_named_fns_match_engine`.
pub const NAMED_FNS: &[&str] = &[
    "sin",
    "cos",
    "tan",
    "asin",
    "arcsin",
    "acos",
    "arccos",
    "atan",
    "arctan",
    "parabola",
    "sq",
    "square",
    "cubic",
    "cube",
    "line",
    "id",
    "identity",
    "abs",
    "exp",
    "sqrt",
    "log",
    "ln",
    "recip",
    "inv",
    "gauss",
    "bell",
    "sinc",
    "sigmoid",
    "logistic",
    "relu",
    "step",
    "heaviside",
];

/// Build-time control keywords (handled by the parser, not the registry).
pub const KEYWORDS: &[&str] = &[
    "let", "for", "if", "else", "def", "in", "sum", "prod", "min", "max",
];

/// Reserved variable names — canvas dims + constants. Never valid entity ids.
pub const RESERVED_VARS: &[&str] = &["w", "h", "cx", "cy", "pi", "e", "tau", "inf", "infinity"];

/// `canvas(...)` preset names.
pub const CANVAS_PRESETS: &[&str] = &[
    "16:9",
    "1080p",
    "4k",
    "square",
    "portrait",
    "4:5",
    "4:3",
    "widescreen",
    "landscape",
    "720p",
    "fullhd",
    "hd",
    "2160p",
    "1:1",
    "9:16",
    "vertical",
    "story",
    "reel",
    "feed",
];

/// `template(...)` / `--template` names (primary + aliases).
pub const TEMPLATES: &[&str] = &[
    "mono",
    "plain",
    "terminal",
    "paper",
    "blueprint",
    "shorts",
    "monochrome",
    "blackwhite",
    "black-white",
    "bw",
    "blank",
    "clean",
    "neon",
    "shell",
    "print",
    "light",
    "blue",
    "short",
    "punch",
];

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn no_duplicate_builtins() {
        let mut names: Vec<_> = catalog().iter().map(|s| s.name).collect();
        names.sort();
        let n = names.len();
        names.dedup();
        assert_eq!(names.len(), n, "duplicate builtin in catalog");
    }

    #[test]
    fn params_are_optional_suffix() {
        // once a param is optional, the rest must be too (no required-after-optional)
        for s in catalog() {
            let mut seen_opt = false;
            for p in &s.params {
                if p.optional {
                    seen_opt = true;
                } else {
                    assert!(
                        !seen_opt,
                        "{}: required param `{}` after an optional one",
                        s.name, p.name
                    );
                }
            }
        }
    }
}
