# Vectors, fields & coordinates

Each block is the whole file — copy it into `x.manic` and run `manic x.manic` (live) or `--record out` (video).

## vector_field

A magnitude-coloured vector field.

```manic
{{#include ../../examples/vector_field.manic}}
```

<div class="manic-video" data-video="ex-vector_field"></div>

## coordinates

Axes, planes, number lines, polar & complex planes.

```manic
{{#include ../../examples/coordinates.manic}}
```

<div class="manic-video" data-video="ex-coordinates"></div>

## pie

A pie chart built from sectors.

```manic
{{#include ../../examples/pie.manic}}
```

<div class="manic-video" data-video="ex-pie"></div>
